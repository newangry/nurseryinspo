import { Box, Flex } from "@mantine/core";

const Selector = () => {
    return (
        <Box sx={(theme) =>({
            border: `1px solid ${theme.colors.gray[5]}`
        })}>
            <Flex>
                
            </Flex>
        </Box>
    )
}

export default Selector;